import { eq } from "drizzle-orm";
import { redirect } from "next/navigation";
import { db } from "@/db";
import { userAccessRequests } from "@/db/schema";
import { getCurrentUser } from "@/lib/security/session";
export default async function WaitingPage() { const user = await getCurrentUser(); if (!user) redirect("/login"); const [request] = await db.select({ status: userAccessRequests.status }).from(userAccessRequests).where(eq(userAccessRequests.userId, user.id)).limit(1); if (!request) redirect("/dashboard"); if (request.status === "awaiting_payment" || request.status === "payment_failed") redirect("/onboarding/billing"); if (request.status === "approved") redirect("/dashboard"); return <main className="flex min-h-screen items-center justify-center bg-slate-950 px-6 text-white"><section className="max-w-xl rounded-3xl border border-white/10 bg-white/[0.04] p-8 text-center"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-amber-400/10 text-3xl">⌛</div><p className="mt-6 text-sm font-semibold text-amber-300">ACCESS REQUEST SENT</p><h1 className="mt-2 text-3xl font-bold">Waiting for the TeamFlow Owner</h1><p className="mt-4 leading-7 text-slate-300">Your email is verified. The Owner will choose your workspace and approve you as a Member or Admin.</p><p className="mt-5 text-sm text-slate-500">Members enter free. Only approved Admins continue to subscription payment.</p></section></main>; }
