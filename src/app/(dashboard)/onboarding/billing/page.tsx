import { redirect } from "next/navigation";
import Link from "next/link";
import { OnboardingPaymentButton } from "@/components/onboarding-payment-button";
import { getCurrentUser } from "@/lib/security/session";
export default async function OnboardingBillingPage() { const user = await getCurrentUser(); if (!user) redirect("/login"); if (user.status !== "active") redirect("/verify-email?required=true"); return <main className="min-h-screen bg-slate-950 px-6 py-16 text-white"><div className="mx-auto max-w-2xl"><p className="text-sm font-semibold text-violet-300">START YOUR OWN TEAM</p><h1 className="mt-3 text-4xl font-bold">Become an Owner in your own workspace</h1><p className="mt-4 text-lg text-slate-300">Your roles stay separate in every workspace. Subscribing lets you create a new workspace where you are Owner; it never changes a Member or Admin role assigned to you in another team.</p><section className="mt-8 rounded-3xl border border-violet-400/30 bg-gradient-to-br from-violet-500/15 to-white/[0.04] p-7"><div className="flex items-end justify-between"><div><h2 className="text-2xl font-semibold">Workspace Owner plan</h2><p className="mt-1 text-slate-400">Monthly test subscription</p></div><p className="text-3xl font-bold">₹499<span className="text-sm font-normal text-slate-400">/month</span></p></div><ul className="my-7 space-y-3 text-slate-300"><li>✓ Create a workspace where you are Owner</li><li>✓ Keep your existing roles in other workspaces</li><li>✓ Invite Members and Admins without charging them</li><li>✓ Secure Stripe subscription checkout</li></ul><OnboardingPaymentButton /><Link href="/dashboard" className="mt-4 block text-center text-sm text-slate-400 hover:text-white">Return to dashboard</Link></section></div></main>; }
